# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Firebase (keep firebase_options.dart if using FlutterFire CLI safely,
# but never commit real API keys if your repo is public and policy requires it)
# android/app/google-services.json
# ios/Runner/GoogleService-Info.plist
